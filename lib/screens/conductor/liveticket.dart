import 'package:flutter/material.dart';

class LiveTicketsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Live Tickets'),
      ),
      body: Center(
        child: Text('Live Tickets Page'),
      ),
    );
  }
}